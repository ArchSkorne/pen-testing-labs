def increment(number, by=1):
    return number + by


print(increment(2))
print(increment(2, 3))
# Default Arguments allow you to define a default value for a function parameter
# If the caller does not provide a value for that parameter, the default value is used